import type { QueryClientConfig } from "@tanstack/query-core";
import type { ReactQueryConstants } from "@/constants/reactQuery.types";

const CLIENT_CONFIG: QueryClientConfig = {};

const ALL: ReactQueryConstants = { CLIENT_CONFIG };
export default ALL;