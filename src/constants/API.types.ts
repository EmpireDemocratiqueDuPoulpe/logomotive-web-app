// --- Types -----------------------------------------------------------------------------------------------------------
export type PrebuiltAPIConstants = Readonly<{
	PROTOCOL: string,
	DOMAIN: string,
	PORT: string
}>;