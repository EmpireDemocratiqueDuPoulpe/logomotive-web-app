import { ReactNode } from "react";
import type { Property } from "csstype";

/// --- PropTypes ------------------------------------------------------------------------------------------------------
export type Props = {
	name?: string
	children?: ReactNode
}