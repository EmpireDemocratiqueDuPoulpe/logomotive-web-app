import LogoHistory from "@/utils/LogoInterpreter/LogoHistory/LogoHistory";

/// --- PropTypes ------------------------------------------------------------------------------------------------------
export type Props = {
	history: LogoHistory
}